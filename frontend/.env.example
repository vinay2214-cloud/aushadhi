# Copy to .env.local and fill in. Both are read at build time by Vite.
# In deployed environments these are supplied at runtime as NITRO_API_BASE_URL
# and NITRO_API_KEY instead, and injected into the page by the SSR shell.
VITE_API_BASE_URL=http://localhost:8000
VITE_API_KEY=

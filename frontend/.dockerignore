# Installed inside the image by `bun install`; copying the host tree over it
# would replace linux binaries with the macOS ones from this volume.
node_modules

# Build artefacts are regenerated by `bun run build`.
.output
dist
dist-ssr
.nitro
.tanstack
.vinxi
.wrangler

# AppleDouble sidecars created by macOS on this exFAT volume. Without this they
# are copied into .output/public and served as real routes (/._favicon.svg, ...).
._*
.DS_Store

.git
.gitignore
*.log
Dockerfile
.dockerignore
